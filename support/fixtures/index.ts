/**
 * PSYNAPSYS E2E — Fixture Index
 *
 * Re-exports the merged test object for convenience.
 * Import from here or directly from '../merged-fixtures'.
 *
 * Usage:
 *   import { test, expect } from '../support/fixtures';
 *   import { test, expect } from '@fixtures';  // via tsconfig paths
 */

export { test, expect } from '../merged-fixtures';
